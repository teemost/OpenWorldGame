// Shared mutable touch input state — updated by TouchControls, read by Player
export const touchState = {
  forward: false,
  back: false,
  left: false,
  right: false,
  shoot: false,
  enter: false,
  run: false,
}
